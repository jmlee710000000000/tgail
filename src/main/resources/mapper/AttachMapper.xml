<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="org.study.home.mapper.AttachMapper">
	<select id="getAttachList" resultType="org.study.home.model.AttachImageDTO">
	
		select * from ship_image where shipId = #{shipId}
	
	</select>

  </mapper>